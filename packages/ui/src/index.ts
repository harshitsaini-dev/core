/**
 * @core/ui
 *
 * Terminal-themed React primitives. The visual rules from ADR-007 live here so
 * they are applied once rather than retyped at every call site.
 */

export { THEME } from '@core/shared';
export * from './primitives.js';
